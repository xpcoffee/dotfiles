.DS_Store
.vim/autoload/
.vim/plugged/

# Downloaded by `komorebic fetch-app-specific-configuration` into
# KOMOREBI_CONFIG_HOME (= komorebi/). Generated, not hand-maintained.
komorebi/applications.json
fish/conf.d/
fish/fish_variables
fish/functions/
fish/fish_plugins
fish/completions/
settings.local.json

# Work / employer-specific material (Neko). These dotfiles are public — anything
# tagged work-* stays local and must never be committed.
work-*

# Work tooling kept local. The performance-feedback skill is general and committed;
# its Neko/personal specifics live in references/work-config.md, caught by work-*.
# aiq wraps prod App Insights queries.
bin/aiq
